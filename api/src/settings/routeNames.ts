export const RouteNames = {
	AI: {
		value: 'ai',
		/*BLOG_ID(blogId: string) {
			return {
				value: blogId,
				full: 'blogs/' + blogId,
				POSTS: {
					value: 'posts',
					full: 'blogs/' + blogId + '/posts',
				},
			}
		},*/
	},
}

export default RouteNames
