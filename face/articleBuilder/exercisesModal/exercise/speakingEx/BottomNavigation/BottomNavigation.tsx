// import Button from '../../../../../../ui/Button/Button'
// import SvgIcon from '../../../../../../ui/icons/SvgIcon'
// import { useGetExerciseControl } from '../common/getExerciseControl'
// import s from './BottomNavigation.module.scss'

// Кнопка перехода к следующему предложению или типу упражнения
/*function BottomNavigation() {
	const buttonConfig = useGetExerciseControl()

	if (!buttonConfig.buttonText || !buttonConfig.handler) {
		return null
	}

	return (
		<div className={s.wrapper}>
			<Button
				text={buttonConfig.buttonText}
				size="big"
				rightIcon={<SvgIcon type="arrowRight_3" />}
				onClick={buttonConfig.handler}
				hotKeyNameLeft="Enter"
			/>
		</div>
	)
}*/

// export default BottomNavigation
